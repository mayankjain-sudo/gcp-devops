# In this directory, run the following command to build this builder.
# $ gcloud builds submit . --config=cloudbuild.yaml

# See docs at https://developer.hashicorp.com/packer/tutorials/docker-get-started/get-started-install-cli
steps:
  - id: "Check Version"
    name: 'gcr.io/$PROJECT_ID/packer'
    volumes:
    - name: 'vol1'
      path: '/persistent_volume'
    args: ['--version','--output-file', '/persistent_volume/step-output.txt']
  
  - id: "Read Output"
    name: 'gcr.io/$PROJECT_ID/packer'
    volumes:
    - name: 'vol1'
      path: '/persistent_volume'
    entrypoint: 'bash'
    args: ['cat', '/persistent_volume/step-output.txt']

